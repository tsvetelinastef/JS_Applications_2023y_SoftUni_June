export class NavComponent {
    constructor(sessionSerice, renderHandler, templateFunction, router) {
        this.sessionSerice = sessionSerice;
        this.renderHandler = renderHandler;
        this.templateFunction = templateFunction;
        this.router = router;
    }

    _showView() {
        
    }
}